import { BreedsFilterPipe } from './breeds-filter.pipe';

describe('BreedsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BreedsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
